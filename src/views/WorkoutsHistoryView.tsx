const WorkoutHistoryView = () => {
    //const { timersArray, totalQueueSeconds, totalSecondsPassed, currentTimerIndex } = useContext(TimersContext);

    return (
        <div style={{ textAlign: 'left', paddingTop: '2rem', paddingLeft: '5rem', display: 'flex', gap: '4rem' }}>
            hello world
            {/* <div>
                    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '1rem' }}>
                        <YourWorkoutList
                            timersArray={timersArray}
                            totalQueueSeconds={totalQueueSeconds}
                            totalSecondsPassed={totalSecondsPassed}
                            currentTimerIndex={currentTimerIndex}
                            isEditing={false}
                        />
                    </div>
                </div> */}
        </div>
    );
};
export default WorkoutHistoryView;
